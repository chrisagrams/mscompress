# `mscompress.parquet`

Optional extra (`pip install 'mscompress[parquet]'`). Round-trip MSZ /
MSZX / mzML with Parquet tables.

::: mscompress.parquet.from_parquet

::: mscompress.parquet.to_parquet
