# `mscompress.datasets`

Optional ML dataset adapters.

## PyTorch

Install with `pip install 'mscompress[torch]'`.

::: mscompress.datasets.torch.MSCompressDatasetMember

## JAX / grain

Install with `pip install 'mscompress[jax]'`.

::: mscompress.datasets.jax.MSCompressJaxDataset
