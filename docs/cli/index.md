# CLI

The `mscompress` command-line tool. The same C library that powers the
Python and Node bindings, exposed as a standalone binary.

- **[Quick Start](quickstart.md)** — compress, decompress, extract,
  describe.
- **[Command reference](reference.md)** — every flag, organized.
- **[Build from source](build-from-source.md)** — CMake, platforms, tests.
- **[Exit codes and errors](errors.md)** — what each failure mode means.

## Install

The CLI is currently built from source. See
[Build from source](build-from-source.md) for the full procedure.
