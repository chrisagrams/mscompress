{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C build",
            "command": "make",
            "type": "shell",
            "args": ["debug"],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}